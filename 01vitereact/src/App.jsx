

function App() {
  const [count, setCount] = useState(0)

  return (
   <h1>Chai aur react | Name </h1>
  );
}

export default App
// react native for mobile app 
// react dom used for reactDOM
// react creates it's own dom named virtual DOM
// createroot method inside react dom
// create custom tags in javascript
// app is function which return html and render that
